# Proto file for .env file. Copy this to .env and add your data.
USER_USERNAME=MyBotUserName
USER_PASSWORD=1234
POSTGRES_USER=chatbot # keep
POSTGRES_DB=chatbot # keep
POSTGRES_PASSWORD=1234 # may keep, not that important
BOTMASTER_DEFAULT_BOTS_LUISE_SECRET=Manf's biggest secret # Just take anything.
BOTMASTER_DEFAULT_BOTS_LUISE_YTKEY=asda # Must get from googleapis if you want to use sing.
